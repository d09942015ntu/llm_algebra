## Usage

```sh
source venv_rl/bin/activate #activate venv
python3 dataset_generator.py # generate the dataset
python3 trainer.py # pre-train the model.
python3 evaluator.py # evaluate the model.
```
